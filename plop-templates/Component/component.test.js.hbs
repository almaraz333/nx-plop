describe('{{pascalCase name}} component', () => {
  beforeEach(() => cy.visit(`/iframe.html?id={{lowerCase name}}--primary`));

    it('should render the component', () => {
      cy.contains("{{pascalCase name}}");
    });
});
