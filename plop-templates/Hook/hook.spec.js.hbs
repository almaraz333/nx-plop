import { renderHook, act } from '@testing-library/react';

import { {{name}} } from './{{name}}';

describe('{{name}} tests', () => {
  test('{{name}} exists', () => {
    const { result } = renderHook(() => {{name}}());

    act(() => {
      //call some function in hook i.e. result.current.someFunction();
    });

    expect(result.current.value).toBe(true);
  });
});