describe('{{componentDestination}}: {{pascalCase name}} component', () => {
    beforeEach(() => cy.visit('/{{lowerCase name}}'));

      it('should render the view', () => {
        cy.contains('{{pascalCase name}}');
      });
  });
